// used from portfolio generator
module.exports = templating => {

    templating.forEach(element => {
        generatePage(element)
        console.log(element)
    });

    function generatePage(element) {

        console.log(element)



    }










}